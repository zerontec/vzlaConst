import { NgDroopFileDirective } from './ng-droop-file.directive';

describe('NgDroopFileDirective', () => {
  it('should create an instance', () => {
    const directive = new NgDroopFileDirective();
    expect(directive).toBeTruthy();
  });
});
